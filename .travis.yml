---
language: "python"
services:
  - "docker"
python:
  - 3.7
  - 3.8

before_script:
  - "pip install invoke"
  - "invoke build-test-container -p $TRAVIS_PYTHON_VERSION"
script:
  - "invoke tests -p $TRAVIS_PYTHON_VERSION"
